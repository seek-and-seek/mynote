~~~JAVA
//1.static静态类型变量，只初始化一次，所有的对象共用一套静态变量
//2.常量命名一般大写
//3.instanceof 是 Java 中的一个关键字，用来判断一个对象是否是某个类或其子类、接口或其实现类的实例。
//3.接口接收实现接口的对象，调用实现的方法（向上转型 多态）
//4.不管是成员内部类还是静态内部类，其在其他外部类中访问，需要通过创建对象的方式。
//4.成员内部类和静态内部类的主要区别是访问权限不同
//5.枚举是静态的 枚举==自己创建自己
//6.switch和枚举的匹配（匹配我们定义的枚举对象）
  switch (red){
      case red01:
          System.out.println("匹配到红色");
  }
    }
//7.一定要注意大小写。
//真就是靠真实吧，对别人真更对自己真实，把自己的缺点暴露出来，但也要积极展现自己的优点。
//tring 对象内部用于存储字符的 char 数组成员，也是分配在堆内存中的，并且当该 String 对象被放入字符串常量池时，它的 char 数组依然在堆中。
~~~

~~~java
//遇到一件事情时
//一定明确自己的预算，同时进行市场调研  市场调研（很重要）；
//不要盲目乐观。
//脸皮一定要厚。
~~~

~~~java
低头赶路，敬事如仪 自知自心，其路则明    
~~~

~~~java
编程思想：正确的情况好想，取反得到错误的情况
~~~

~~~java
public static void main(String[] args) {
    String str = "abcdef";

        try {
    String  str1 = reverse(str,0,3);
    System.out.println(str1);
        } catch (Exception e) {
            System.out.println(e.getMessage());
        }
    }
    public static String reverse(String str,int start,int end){
        if(!(str !=null&&start>=0&&start<end&&end<str.length())){
            throw new RuntimeException("参数错误");
        }
        char[] chars = str.toCharArray();
        char temp;
        for (int i = start,  j = end; i < j ; i++,j--) {
            temp = chars[i];
            chars[i]=  chars[j];
            chars[j] = temp;
        }
        return new String(chars);
    }
//try-catch里面的变量作用域在里面
//throw 抛出异常对象
//toCharArray（）方法，string-->char
~~~

~~~~java
//抢任何新东西，一定要看攻略。
~~~~

